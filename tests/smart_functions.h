#ifndef SMART_FUNCTIONS_H
#define SMART_FUNCTIONS_H

namespace sf {
template <typename T>
T add(T a, T b) {
    return a + b;
}

template <typename T>
T mult(T a, T b) {
    return a * b;
}
}

#endif /* SMART_FUNCTIONS_H */
